services:
  - type: web
    name: kairon-attendance-assistant
    runtime: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r backend/requirements.txt && python -m playwright install chromium
    startCommand: cd backend && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 180
    healthCheckPath: /api/config
    autoDeployTrigger: commit
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: "0"
      - key: PYTHON_VERSION
        value: "3.12.4"
      - key: HOST
        value: "0.0.0.0"
      - key: roll_no
        sync: false
      - key: password
        sync: false
